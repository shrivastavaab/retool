export type { TableHeadProps, TableBodyProps, TableFooterProps, TableCellProps, } from "@mui/material";
export type TableType = {
    text: string;
    body: string;
    default: {
        body: string;
        border: string;
    };
    focused: {
        body: string;
    };
};
export type { TableContainerProps, TableRowProps } from "@mui/material";
