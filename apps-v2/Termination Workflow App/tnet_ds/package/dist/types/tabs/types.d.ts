export type TabType = {
    text: {
        default: string;
        selectedHover: string;
        active: string;
        disabled: string;
    };
    background: {
        default: string;
        hover: string;
        active: string;
    };
    border: string;
};
