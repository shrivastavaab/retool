import type { DatePickerProps } from "./types";
import React from "react";
export declare const DatePicker: ({ slots, slotProps, format, disabledDates, shouldDisableDate, ...props }: DatePickerProps) => React.ReactElement;
